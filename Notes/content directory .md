├── content/
│   ├── main.js            ← Entry point + router (switch-case)
│   ├── textSender.js      ← Text-only sending logic
│   ├── imageSender.js     ← Image sending logic (paste + attach fallback)
│   ├── captionHelper.js   ← Caption typing (6 methods)
│   ├── uiHelpers.js       ← Button/input finders
│   └── utils.js           ← Shared utilities (delay, click, wait)